# Qualified package payloads are byte-addressed and must never be rewritten by
# checkout EOL conversion or whitespace normalization.
web-app/vendor/nirs4all/** -text -eol -whitespace
web-app/src/engine/wasm/** -text -eol -whitespace
web-app/public/legal/** -text -eol -whitespace
