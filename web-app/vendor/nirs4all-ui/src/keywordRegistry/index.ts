/**
 * Keyword/effect registry foundation — public surface.
 *
 * Pure helpers for consuming the full Python `nirs4all` keyword registry as a
 * metadata contract in Studio/Web hosts. This layer validates and projects the
 * registry document; it does not execute keywords or infer runtime support.
 */

export * from "./registry.js";
